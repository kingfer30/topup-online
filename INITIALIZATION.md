# 系统初始化指南

本文档说明如何初始化Topup Online系统。

## 初始化流程

### 1. 准备工作

在开始初始化之前，请确保：

1. **数据库准备**
   - 已安装MySQL数据库（推荐5.7+或8.0+）
   - 创建一个空数据库用于系统使用
   ```sql
   CREATE DATABASE topup_online CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```
   - 确保数据库用户有足够的权限

2. **服务启动**
   - 后端服务已启动（端口8080）
   - 前端Admin项目已启动（端口3001）

### 2. 访问初始化页面

首次启动系统时，访问后台管理系统会自动检测初始化状态：

```
http://localhost:3001
```

如果系统未初始化，会自动跳转到初始化页面：

```
http://localhost:3001/initialize
```

### 3. 步骤一：数据库配置

在初始化页面的第一步，填写数据库连接信息：

| 字段 | 说明 | 示例 |
|------|------|------|
| 数据库地址 | MySQL服务器地址 | localhost |
| 数据库端口 | MySQL服务器端口 | 3306 |
| 数据库名称 | 数据库名称 | topup_online |
| 数据库用户名 | 数据库用户 | root |
| 数据库密码 | 数据库密码 | your_password |

**重要步骤：**
1. 填写完所有数据库信息后
2. 点击 **"测试连接"** 按钮
3. 确保显示"数据库连接成功"提示
4. 然后点击 **"下一步"** 进入管理员设置

### 4. 步骤二：管理员设置

配置系统的第一个管理员账号：

| 字段 | 说明 | 要求 |
|------|------|------|
| 管理员用户名 | 登录用户名 | 建议使用admin |
| 管理员密码 | 登录密码 | 至少6位字符 |
| 确认密码 | 再次输入密码 | 必须与密码一致 |
| 管理员邮箱 | 管理员邮箱 | 有效的邮箱格式 |

填写完成后，点击 **"开始初始化"** 按钮。

### 5. 步骤三：初始化完成

系统会自动执行以下操作：

1. ✅ 连接数据库
2. ✅ 创建数据表结构：
   - `system_config` - 系统配置表
   - `admins` - 管理员表
   - `users` - 用户表
   - `orders` - 订单表
   - `cards` - 卡密表
3. ✅ 创建管理员账号
4. ✅ 保存系统配置
5. ✅ 生成配置文件

初始化成功后，点击 **"前往登录"** 按钮，使用刚才创建的管理员账号登录系统。

## 数据表结构

系统初始化会创建以下数据表：

### system_config - 系统配置表
```sql
- id: 主键
- key: 配置键
- value: 配置值
- created_at: 创建时间
- updated_at: 更新时间
```

### admins - 管理员表
```sql
- id: 主键
- username: 用户名
- password: 密码（MD5加密）
- email: 邮箱
- status: 状态（1:正常 0:禁用）
- created_at: 创建时间
- updated_at: 更新时间
```

### users - 用户表
```sql
- id: 主键
- username: 用户名
- email: 邮箱
- ... (其他字段根据实际需求)
```

### orders - 订单表
```sql
- id: 主键
- order_no: 订单号
- user_email: 用户邮箱
- amount: 金额
- status: 状态（pending/processing/completed/cancelled/refunded）
- card_code: 卡密
- completed_at: 完成时间
- note: 备注
- created_at: 创建时间
- updated_at: 更新时间
```

### cards - 卡密表
```sql
- id: 主键
- code: 卡密代码
- type: 卡密类型
- value: 面值（天数）
- status: 状态（0:未使用 1:已使用 2:已过期）
- used_by: 使用者邮箱
- used_at: 使用时间
- expired_at: 过期时间
- created_at: 创建时间
- updated_at: 更新时间
```

## 生成的配置文件

初始化完成后，系统会在后端目录生成以下文件：

### `.env` - 环境配置文件
```env
DB_HOST=localhost
DB_PORT=3306
DB_NAME=topup_online
DB_USER=root
DB_PASSWORD=your_password
```

### `.initialized` - 初始化标记文件
此文件用于标记系统已完成初始化，防止重复初始化。

## 常见问题

### Q1: 数据库连接测试失败？

**可能原因：**
- 数据库服务未启动
- 数据库地址或端口不正确
- 数据库用户名或密码错误
- 数据库用户权限不足

**解决方法：**
1. 检查MySQL服务是否运行
2. 确认数据库连接信息是否正确
3. 确保数据库用户有足够权限

### Q2: 初始化失败？

**可能原因：**
- 数据库用户权限不足
- 数据库已存在同名表
- 网络连接问题

**解决方法：**
1. 检查数据库用户是否有CREATE TABLE权限
2. 如果是测试环境，可以删除旧表后重试
3. 检查后端服务日志获取详细错误信息

### Q3: 如何重新初始化？

如果需要重新初始化系统：

1. **备份数据**（如果需要）
2. 删除后端目录下的初始化标记文件：
   ```bash
   rm backend/.initialized
   ```
3. 删除环境配置文件：
   ```bash
   rm backend/.env
   ```
4. 清空或删除数据库：
   ```sql
   DROP DATABASE topup_online;
   CREATE DATABASE topup_online CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```
5. 重新访问系统进行初始化

### Q4: 密码是如何加密的？

系统使用 **MD5** 加密算法对管理员密码进行加密：
- 前端：在提交前对密码进行MD5加密
- 后端：接收MD5加密后的密码存储到数据库
- 登录验证：对输入密码进行MD5加密后与数据库对比

**注意：** MD5加密不可逆，忘记密码需要通过数据库重置。

## 安全建议

1. **生产环境**：
   - 使用强密码
   - 定期更换管理员密码
   - 限制数据库访问权限
   - 启用防火墙

2. **数据库安全**：
   - 不要使用root用户
   - 创建专门的数据库用户
   - 限制远程访问

3. **文件权限**：
   - `.env` 文件应设置适当的权限
   - `.initialized` 文件防止被修改

## 后续步骤

初始化完成后，您可以：

1. 登录后台管理系统
2. 在系统设置中配置支付方式
3. 在系统设置中配置通知设置
4. 开始使用系统功能

## 技术支持

如有问题，请查看：
- 项目README.md
- 后端日志文件
- 前端控制台输出

或联系技术支持：support@example.com

